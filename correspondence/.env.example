# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=            # server only — never expose

# Anthropic (Guardian module; server only)
ANTHROPIC_API_KEY=

# Used when minting the archive magic link (falls back to request origin)
NEXT_PUBLIC_SITE_URL=

# Used by `npm run seed` to stamp the writer role on your account
WRITER_EMAIL=
