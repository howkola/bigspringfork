# seed-docs

The project's canon lives here **locally only** — it is excluded from git
while this repository is public, because it contains surprises for a real
person. See `../seeds/README.md` for how to restore it from the private
bundle, and the handoff README for why this must never be indexable.
